# Core Rules (Generated)

## Story Flags
{% for f in flags %}- `{{ f.id }}` ({{ f.label }}): {{ "ON" if f.enabled else "OFF" }}
{% endfor %}

## Boundary States
{% for name, rule in rules.items() %}- `{{ name }}`: `{{ rule.expression }}`
  - {{ rule.notes }}
{% endfor %}

## Maintenance Levels
- Camp: default={{ maintenance.levels.camp.default }}, joined_act2={{ maintenance.levels.camp.joined_act2 }}, field_lv2_unlocked={{ maintenance.levels.camp.field_lv2_unlocked }}
- Base: default={{ maintenance.levels.base.default }}, joined_act2={{ maintenance.levels.base.joined_act2 }}, shrine_lv3_unlocked={{ maintenance.levels.base.shrine_lv3_unlocked }}
